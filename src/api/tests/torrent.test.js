import chai, { expect } from 'chai'

describe('## Torrent APIs', () => {
  describe('# POST Seed a video', () => {
    it('ok', (done) => {
      (async() => {
        done()
      })()
    })
  })
})
